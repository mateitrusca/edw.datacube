PREFIX dc: <http://purl.org/dc/terms/>
PREFIX qb: <http://purl.org/linked-data/cube#>
SELECT DISTINCT * WHERE {
  {{ dataset.n3() }}
    dc:title ?title ;
    dc:description ?description ;
	dc:identifier ?identifier .
    OPTIONAL { {{ dataset.n3() }} dc:license ?license}
}
LIMIT 100
